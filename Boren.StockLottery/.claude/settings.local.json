{
  "permissions": {
    "allow": [
      "Bash(dotnet --version 2>/dev/null || echo \"dotnet CLI not in path\")",
      "WebSearch",
      "WebFetch(domain:www.ibfs.com.tw)",
      "Bash(dotnet add:*)"
    ]
  }
}
